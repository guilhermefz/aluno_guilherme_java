spring:
  application:
    name: ALUNO_guilherme
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/alunos
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  server:
    servlet:
      encoding:
        charset: UTF-8
        enabled: true
        force: true